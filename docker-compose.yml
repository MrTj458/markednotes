version: '3.9'

services:
  app:
    build: .
    command: reflex -r '(\.go$$|go\.mod)' -s go run cmd/markednotes/main.go
    volumes: 
      - .:/app
    ports: 
      - "8080:8080"
    environment:
      - DATABASE_URL=postgres://postgres:postgres@db:5432/markednotes
      - PORT=8080
    depends_on:
      - db

  db:
    image: postgres:alpine
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=markednotes
      - POSTGRES_PASSWORD=postgres

volumes:
  db_data:
